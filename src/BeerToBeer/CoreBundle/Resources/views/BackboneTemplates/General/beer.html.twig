<div class="beer">
	<%= name %><br />
	<span class="prices">
		<% _.each(prix, function (oneprice, key, list) { 
			if (key == list.length-1) { %>
				<span class="price lastPrice">
			<% } else { %>
				<span class="price">
			<% } %>
				<span class="volume"><%= oneprice.volume %> cl</span>
				<%= oneprice.prixNormal %> €
			</span>
		<% }); %>
	</span>
	<div class="pull-right">
		<a href="#" class="editBeerLink" id="<%= id %>">Modifier</a>
	</div>
</div>
